// API produit Popover: composition officielle shadcn/Base UI. Le trigger doit
// appartenir au Root afin que positionnement, dismiss et retour focus partagent
// la même machine d'état.
export {
  Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverHeader,
  PopoverTitle,
  PopoverDescription,
} from "../shadcn/popover";
